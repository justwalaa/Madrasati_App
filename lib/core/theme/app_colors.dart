import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Brand / UI
  static const Color navyDark = Color(0xFF0B1930);
  static const Color navy = Color(0xFF0F2340);

  static const Color primary = Color(0xFF0F2340);
  static const Color secondary = Color(0xFF43D6D6);

  // Backgrounds
  static const Color background = Color(0xFFF5F7FB);
  static const Color surface = Color(0xFFFFFFFF);

  // Cards / Inputs
  static const Color aquaSoft = Color(0xFFCFF3F3);
  static const Color aquaBorder = Color(0xFF7FE0E0);
  static const Color border = Color(0xFFCED7E2);

  // Text
  static const Color textPrimary = Color(0xFF0B1930);
  static const Color textMuted = Color(0xFF6C7A92);
  static const Color textOnDark = Color(0xFFFFFFFF);

  // Status
  static const Color error = Color(0xFFE53935);
  static const Color success = Color(0xFF2E7D32);
}
